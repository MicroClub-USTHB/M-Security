// Placeholder for Rust traits exposed via FRB.
// These are opaque on the Dart side — only the Rust implementations matter.

abstract class Encryption {}

abstract class Hasher {}

abstract class Kdf {}
